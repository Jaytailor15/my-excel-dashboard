import "./globals.css";

export const metadata = {
  title: "My Excel Dashboard",
  description: "My Excel Dashboard",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
